import 'package:flutter/cupertino.dart';

class CameraRouter extends StatefulWidget {
  const CameraRouter({Key? key}) : super(key: key);

  @override
  State<CameraRouter> createState() => _CameraRouterState();
}

class _CameraRouterState extends State<CameraRouter> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
